apiVersion: v1
kind: ConfigMap
metadata:
  name: log-auto-pipeline
data:
  logstash.conf: |-
    input {
      tcp {
          port => "5044"
          codec => json_lines
      }
    }

    output {
      stdout {}
      elasticsearch {
        hosts => ["http://{{ .Values.global.hostIp }}:31996"]
        index => "producer-elk-tcp-%{+YYYY.MM.dd}"
      }
    }