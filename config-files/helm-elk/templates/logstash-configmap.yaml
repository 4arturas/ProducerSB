apiVersion: v1
kind: ConfigMap
metadata:
  name: log-auto-pipeline
data:
  logstash.yml: |
    http.host: "0.0.0.0"
    path.config: /usr/share/logstash/pipeline
  logstash.conf: |-
    input {
      tcp {
          port => "5044"
          codec => json_lines
      }
    }

    output {
      stdout {}
      elasticsearch {
        hosts => "192.168.56.10:31996"
        index => "producer-elk-tcp-%{+YYYY.MM.dd}"
      }
    }