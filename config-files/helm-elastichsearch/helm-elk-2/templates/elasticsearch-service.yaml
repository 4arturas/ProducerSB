apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-logging
  labels:
    k8s-app: elasticsearch-logging
spec:
  type: NodePort
  ports:
    - port: 9200
      protocol: TCP
      targetPort: 9200
      nodePort: 31996
  selector:
    k8s-app: elasticsearch-logging
